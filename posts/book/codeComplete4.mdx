---
title: "코드 컴플리트4"
category: "book"
date: "2022-07-31"
---

## 14장 순차적 코드 구성하기

1. 순서가 중요한 명령문

- 의존성이 분명하게 보이게 코드를 구성하라
- 의존성이 분명하게 보이게 루틴의 이름을 작성하라
- 의존성을 분명히 하기 위해서 루틴 매개변수를 사용하라
- 의존성이 분명하지 않은 부분은 주석으로 문서화하라
- 어설션이나 오류 처리 코드로 의존성을 검사하라

2. 순서가 중요하지 않은 명령문

- 코드를 하향식으로 읽을 수 있도록 개발
- 연관된 명령문 그룹화하기

## 15장 조건문 사용

1. if문

- 일반적인 경우를 처리하는 코드를 먼저 작성한 다음 특별한 경우를 처리하라
- 동치에 대해서 정확하게 이동 경로를 결정하라
- 정상적인 경우를 else가 아닌 if 문 다음에 입력하라
- if문 다음에 의미있는 명령문을 작성하라
- else절을 고려하라

2. case문

- 알파벳 순이나 숫자 순으로 case를 나열하라
- 가장 정상적인 경우를 앞에 놓아라
- 빈도에 따라서 case를 나열하라
- case문이 하느 일을 간단하게 유지하라
- case문에서 사용하기 위한 가짜 변수를 만들지 않는다.
- 유효한 기본값을 찾고자 하는 경우에만 default 문을 사용한다.
- 오류를 검출하기 위해서 default문을 사용한다.

## 16장 반복문 제어

1. 반복문 종류 선택

- while: 반복문을 몇 번 반복하고 싶은지 정확하게 알지 못할 때 사용, 시작부분에서 테스트 하는 반복문, 끝에서 테스트 하는 반복문
- 탈출이 가능한 반복문 : 반복문의 중간에서 종료되는 경우 while-break
- for: 지정된 횟수만큼 실행하는 반복문
- forEach: 배열이나 다른 컨테이너의 각 멤버에 대해서 연산을 수행하는 경우

2. 반복문 제어

- 반복문 집입
  - 한 위치에서만 반복문에 진입하라
  - 반복문을 시작하기 바로 전에 초기화 코드를 입력하라
  - 무한 반복문에는 while를 사용하라
  - 적절하다면 for 반복문을 사용하라
  - while 반복문을 사용하는 게 더 적절할 때 for 반복문을 사용하지 말라
- 반복문의 중간 부분 처리하기
  - 반복문에서 보조 관리 작업은 반복문의 시작이나 끝에 놓아라
  - 반복문이 하나의 기능만 처리하게 하라
- 반복문 종료
  - 반복문이 종료되는지 확인하라
  - 반복문 종료 조건을 분명히 하라
  - 반복문을 종료하기 위해서 for 반복문의 인덱스를 조작하지 말라.
  - 반복문 인덱스의 마지막 값에 의존하는 코드를 피하라
- 반복문을 일찍 탈출하기
  - while 반복문에서 불린 플래그를 사용하는 대신 break 명령문을 사용하는 것을 고려한다.
  - 반복문 내에 수많은 break문이 사용되는 것을 주의한다.
  - 반복문의 앞부분에서 테스트를 위한 목적으로 coutinue를 사용한다.
  - 언어가 지원한다면 레이블 break 구조를 사용한다.
  - break와 continue를 신중하게 사용한다.
- 종결점 확인
- 반복문 변수 사용
  - 배열과 반복문에 한계를 두기 위해서 서수나 열거형을 사용하라
  - 중첩 반복문을 읽기 쉽게 하기 위해서 의미 있는 변수 이름을 사용하라
  - 반복문 인덱스끼리 혼선을 피하기 위해서 의미 있는 이름을 사용하라
  - 반복문 인텍스 변수의 범위를 반복문 자체로 제한하라

3. 반복문을 쉽게 작성하는 방법

4. 반복문과 배열의 연관성

## 17장 특이한 제어 구조

1. 여러 곳에서 반환하는 루틴

- 더 읽기 쉬운 코드를 만들기 위해서 return 을 사용하라
- 복잡한 오류 처리를 단순화하기 위해 보호절을 사용하라
- 한 루틴에 있는 리턴의 수를 최소화하라

2. 재귀문

- 한 루틴이 문제의 작은 부분을 해결하고 그 문제를 더 작은 부분으로 나눈 다음, 자신을 호출해 작게 나뉜 각 문제를 해결한다.
- 재귀 호출 사용 팁
  - 무한 재귀 호출을 막기 위해서 안전 카운터를 사용하라
  - 한 루틴이 재귀 호출을 제한하라
  - 스택을 감시하라
  - 팩토리얼이나 피보나치 수열을 계산하기 위해서 재귀문을 사용하지 말라

3. goto문
4. 특이한 제어 구조에 대한 관점

## 18장 테이블 활용 기법

1. 테이블 활용 기법에서 일반적으로 고려해야 할 사항

- 테이블을 활용한 기법의 두 가지 문제점
  - 테이블에 있는 엔트리를 어떻게 참조할 것인가에 대한 질문에 답할 수 있어야 한다.
  - 테이블 활용한 기법을 사용하며 테이블에 무엇을 저장해야 할지 설명해야 한다.

2. 직접 접근 방식 (월별 일수 예제, 보험료 예제, 유연한 메세지 형식 예제 )

- 논리적 접근 방식
- 객체지향적인 접근법
- 테이블을 활용한 접근법

3. 인덱스 접근 방식

4. 단계적 접근 방식

- 종결점을 확인하라
- 순차 검색법 대신 이진 검색법 사용을 고려해보라
- 단계적 접근 방식 대신 인덱스 접근 방식 사용을 고려해보라

5. 그 밖의 테이블 참조 방법

## 19장 제어과 관련된 일반적인 이슈

1. 불린 표현식

- boolean 테스트에서 true와 false 사용하기
  - boolean 값을 암묵적으로 true와 false에 비교하라
- 복잡한 표현식을 단순하게 만들기
  - 복잡한 테스트를 새로운 불린 변수를 사용해 부분적인 테스트로 나눠라
  - 복잡한 표현식을 불린 함수로 옮겨라
  - 의사결정 테이블을 사용해 복잡한 조건을 대체하라
- 불린 표현식을 긍정문을 작성하기
  - 드로므간의 법칙을 적용해 부정 불린 테스트를 단순화하라
- 괄호를 사용해 이해하기 쉬운 불린 표현식 만들기
  - 괄호의 균형을 유지하기 위해서 간단한 계산 기법을 사용하라
  - 논리 표현식에는 충분할 정도로 괄호를 사용하라
- 불린 표현식 평가 방법
- 숫자의 크기 순서대로 수치 표현식 작성하기
- 0을 비교하는 방법
- 불린 표현식과 관련된 일반적인 문제

2. 복합문(블록)

- 중괄호 쌍을 함께 작성하라
- 중괄호를 사용하여 조건문을 분명히 하라

3. Null 명령문

4. 지나치게 깊은 중럽 구조 처리

- 조건의 일부분을 다시 테스트하여 중첩된 if문을 단순화하라
- break 블록을 사용해 중첩된 if문을 단순화하라
- 중첩된 if문을 if-then-case 문으로 변환하라
- 중첩된 if문을 case문으로 변환하라
- 중첩 구조가 깊은 코드를 루틴으로 작성하라
- 좀 더 객체지향적인 방법으로 접근하라
- 깊게 중첩되는 코드는 재설계하라

5. 프로그래밍의 기초 : 구조적 프로그래밍

- 구조적 프로그래밍의 3가지 요소 : 순차적 명령문, 선택, 반복

6. 제어 구조와 복잡성
   9
